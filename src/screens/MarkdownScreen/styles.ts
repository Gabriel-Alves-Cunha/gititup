import styled from "styled-components/native";

export const Container = styled.View`
	flex: 1;
`;

export const BuildingMarkdown = styled.View`
	position: absolute;
	flex: 1;

	margin-left: auto;
	margin-right: auto;
	margin-top: auto;
	margin-bottom: auto;

	left: 0;
	right: 0;
	top: 0;
	bottom: 0;

	justify-content: center;

	color: #0612f5;
`;
